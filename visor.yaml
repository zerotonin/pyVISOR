# visor.yaml 

# install: conda env create -f charon.yaml 
# update:  conda env update -f charon.yaml 
name: visor
dependencies:
  - python=3.7
  - pip
  #- pygame
  - pillow
  #- av
  #- moviepy
  - ipython
  - pandas
  - opencv
  - matplotlib
  - pims
  - tqdm
  - qt
  - pip:
      - pillow
      - matplotlib
      - pandas
      - opencv-python
      - PyQt5

